{
  "id": "categories",
  "name": "Categories",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_categories",
      "name": "Categories",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "onboarding_landing-9oxh",
      "schema": "{{task.onboarding_landing-9oxh:null$_$dbo.schema}}",
      "database": "{{task.onboarding_landing-9oxh:null$_$dbo.database}}"
    }
  ]
}
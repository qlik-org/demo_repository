{
  "id": "customers",
  "name": "Customers",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_customers",
      "name": "Customers",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "replication-9hg4",
      "schema": "{{task.replication-9hg4:null$_$dbo.schema}}",
      "database": "{{task.replication-9hg4:null$_$dbo.database}}"
    }
  ]
}
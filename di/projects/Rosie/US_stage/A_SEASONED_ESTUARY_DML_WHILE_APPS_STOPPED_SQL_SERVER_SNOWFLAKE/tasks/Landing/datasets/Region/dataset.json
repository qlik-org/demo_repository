{
  "id": "region",
  "name": "Region",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_region",
      "name": "Region",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "landing-bw2h",
      "schema": "{{task.landing-bw2h:null$_$dbo.schema}}",
      "database": "{{task.landing-bw2h:null$_$dbo.database}}"
    }
  ]
}
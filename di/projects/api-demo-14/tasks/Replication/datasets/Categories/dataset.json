{
  "id": "categories",
  "name": "Categories",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_categories",
      "name": "Categories",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "replication-optb",
      "schema": "{{task.replication-optb:null$_$dbo.schema}}",
      "database": "{{task.replication-optb:null$_$dbo.database}}"
    }
  ]
}
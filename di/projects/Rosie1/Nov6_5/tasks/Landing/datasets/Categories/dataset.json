{
  "id": "categories",
  "name": "Categories",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_categories",
      "name": "Categories",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "landing-nsdz",
      "schema": "{{task.landing-nsdz:null$_$dbo.schema}}",
      "database": "{{task.landing-nsdz:null$_$dbo.database}}"
    }
  ]
}
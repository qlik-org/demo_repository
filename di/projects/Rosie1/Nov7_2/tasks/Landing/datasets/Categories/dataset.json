{
  "id": "categories",
  "name": "Categories",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_categories",
      "name": "Categories",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "landing-nsdt",
      "schema": "{{task.landing-nsdt:null$_$dbo.schema}}",
      "database": "{{task.landing-nsdt:null$_$dbo.database}}"
    }
  ]
}
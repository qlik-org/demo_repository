{
  "id": "customers",
  "name": "Customers",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_customers",
      "name": "Customers",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "replication1804-x-cv",
      "schema": "{{task.replication1804-x-cv:null$_$dbo.schema}}",
      "database": "{{task.replication1804-x-cv:null$_$dbo.database}}"
    }
  ]
}
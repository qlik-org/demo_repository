properties:
  name: Replication
  id: replication-hyix
  type: REPLICATION
settings:
  targetEndpoint:
    targetConnection: '{{task.replication-hyix.targetConnection}}'
    targetStorageConnection: '{{task.replication-hyix.targetStorageConnection}}'
  taskSettings:
    fullLoad: true
    applyChanges: true

properties:
  id: customers
  name: Customers
transformations:
  columnTransformations:
    - action: KEEP
      columnName: FullAddress
      expression:
        expressionStatement: $CONCAT(${X}, $CONCAT(' ', ${Y}))
        alias:
          - name: X
            value: address
          - name: Y
            value: city
      ordinal: 12
sourceTable:
  schema: '{{task.landing-tti4:null$_$dbo.schema}}'
  name: Customers

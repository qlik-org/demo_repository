properties:
  name: Registered_data
  id: registered_data-bu-g
  type: REGISTERED_DATA
settings:
  artifactsLocation:
    internalSchema: '{{task.registered_data-bu-g.internalSchema}}'
    taskSchema: '{{task.registered_data-bu-g.taskSchema}}'
    databaseName: '{{task.registered_data-bu-g.databaseName}}'

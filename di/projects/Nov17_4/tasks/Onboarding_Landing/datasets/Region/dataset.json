{
  "id": "region",
  "name": "Region",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_region",
      "name": "Region",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "onboarding_landing-9la3",
      "schema": "{{task.onboarding_landing-9la3:null$_$dbo.schema}}",
      "database": "{{task.onboarding_landing-9la3:null$_$dbo.database}}"
    }
  ]
}
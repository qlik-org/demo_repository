{
  "id": "categories",
  "name": "Categories",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_categories",
      "name": "Categories",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "replication-b80l",
      "schema": "{{task.replication-b80l:null$_$dbo.schema}}",
      "database": "{{task.replication-b80l:null$_$dbo.database}}"
    }
  ]
}
{
  "id": "orders",
  "name": "Orders",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_orders",
      "name": "Orders",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "landing-exgh",
      "schema": "{{task.landing-exgh:null$_$dbo.schema}}",
      "database": "{{task.landing-exgh:null$_$dbo.database}}"
    }
  ]
}
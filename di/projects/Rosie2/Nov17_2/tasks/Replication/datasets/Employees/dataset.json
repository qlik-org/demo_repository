{
  "id": "employees",
  "name": "Employees",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_employees",
      "name": "Employees",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "replication-a0x3",
      "schema": "{{task.replication-a0x3:null$_$dbo.schema}}",
      "database": "{{task.replication-a0x3:null$_$dbo.database}}"
    }
  ]
}
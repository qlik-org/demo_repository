{
  "id": "products",
  "name": "Products",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_products",
      "name": "Products",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "replication_1-xqbv",
      "schema": "{{task.replication_1-xqbv:null$_$dbo.schema}}",
      "database": "{{task.replication_1-xqbv:null$_$dbo.database}}"
    }
  ]
}
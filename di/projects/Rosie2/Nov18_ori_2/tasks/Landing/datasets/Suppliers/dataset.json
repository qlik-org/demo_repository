{
  "id": "suppliers",
  "name": "Suppliers",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_suppliers",
      "name": "Suppliers",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "landing-nedr",
      "schema": "{{task.landing-nedr:null$_$dbo.schema}}",
      "database": "{{task.landing-nedr:null$_$dbo.database}}"
    }
  ]
}
{
  "id": "region",
  "name": "Region",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_region",
      "name": "Region",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "lake_landing-d1cn",
      "schema": "{{task.lake_landing-d1cn:null$_$dbo.schema}}",
      "database": "{{task.lake_landing-d1cn:null$_$dbo.database}}"
    }
  ]
}
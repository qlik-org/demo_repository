{
  "id": "territories",
  "name": "Territories",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_territories",
      "name": "Territories",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "landing-in3j",
      "schema": "{{task.landing-in3j:null$_$dbo.schema}}",
      "database": "{{task.landing-in3j:null$_$dbo.database}}"
    }
  ]
}
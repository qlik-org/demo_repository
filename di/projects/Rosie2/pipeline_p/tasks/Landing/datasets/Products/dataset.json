{
  "id": "products",
  "name": "Products",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_products",
      "name": "Products",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "landing-pepz",
      "schema": "{{task.landing-pepz:null$_$dbo.schema}}",
      "database": "{{task.landing-pepz:null$_$dbo.database}}"
    }
  ]
}
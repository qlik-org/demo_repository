{
  "id": "order_details",
  "name": "Order Details",
  "type": "LANDING",
  "inputDatasets": [
    {
      "datasetId": "$replicate$dbo_order_details",
      "name": "Order Details",
      "projectId": "{{ref(project.current.projectId)}}",
      "taskId": "onboarding_landing-pepm",
      "schema": "{{task.onboarding_landing-pepm:null$_$dbo.schema}}",
      "database": "{{task.onboarding_landing-pepm:null$_$dbo.database}}"
    }
  ]
}